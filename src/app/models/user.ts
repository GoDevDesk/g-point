export interface User {
    id: number,
    userName: string,
    emailAddress: string,
    firstName: string,
    lastName: string
}

export interface authUser{
    email:string,
    password:string
}